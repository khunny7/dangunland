{
  "name": "dangunland-mud-client",
  "version": "1.0.0",
  "description": "Web-based MUD client for DangunLand with retro 80s computer aesthetic",
  "private": true,
  "type": "module",
  "main": "backend/src/server.js",
  "workspaces": [
    "backend",
    "frontend",
    "frontend-react"
  ],
  "scripts": {
    "dev": "concurrently \"npm --workspace backend run dev\" \"npm --workspace frontend-react run dev\"",
    "build": "npm --workspace frontend-react run build",
    "start": "node backend/src/server.js",
    "install-all": "npm install && npm --workspace backend install && npm --workspace frontend-react install"
  },
  "keywords": [
    "mud",
    "client",
    "telnet",
    "websocket",
    "retro",
    "terminal"
  ],
  "author": "khunny7",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
